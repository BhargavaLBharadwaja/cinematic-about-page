import * as THREE from 'three';
export class AstronautInteraction { private target=new THREE.Vector3(); private targetRotation=new THREE.Euler(); update(model:THREE.Group,pointer:THREE.Vector2,reducedMotion:boolean,delta:number){const amount=reducedMotion?0:.55; this.target.set(pointer.x*.12*amount,-pointer.y*.1*amount,0); model.position.lerp(this.target,1-Math.exp(-4*delta)); this.targetRotation.set(-pointer.y*.045*amount,pointer.x*.075*amount,pointer.x*.018*amount); model.rotation.x=THREE.MathUtils.damp(model.rotation.x,this.targetRotation.x,4,delta); model.rotation.y=THREE.MathUtils.damp(model.rotation.y,this.targetRotation.y,4,delta); model.rotation.z=THREE.MathUtils.damp(model.rotation.z,this.targetRotation.z,4,delta); } }
